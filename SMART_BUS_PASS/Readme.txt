Smart Bus Pass QR Scanner

This project is a Smart Bus Pass QR Code Scanner system built with HTML5, Node.js (Express), and MongoDB.
It allows scanning student bus pass QR codes, validating them, and storing the details in a database.

📌 Features

Scan QR codes using HTML5 QR Code Scanner

Parse student details (Name, Email, College, ID, Pass type, Route, Validity)

Show valid/invalid pass status on UI

Save scanned details to MongoDB via Express backend

Prevent duplicate entries for the same student & pass